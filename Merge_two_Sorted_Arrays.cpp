#include<bits/stdc++.h>
using namespace std;
#define nline "\n"

int main()
{
	vector<int>v1 = {1, 2, 3};
	vector<int>v2 = {2, 5, 6};
	int m = 3, n = 3;
//MY WAY:
	/*vector<int>ans;
	for (int i = 0 ; i < m ; i++) {
		ans.push_back(v1[i]);
		ans.push_back(v2[i]);
	}
	sort(ans.begin(), ans.end());
	for (auto it : ans) {
		cout << it << " ";
	}*/

//Two Pointer:
	cout << (2 < 2);


	return 0;
}